---
- name: "Check hosts in the cluster"
  hosts: 127.0.0.1
  connection: local
  tasks:
    - fail:
        msg: "There are no hosts in the {{ type_name }}"
      when: groups[ group_name ] | length == 0
